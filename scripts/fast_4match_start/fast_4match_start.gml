/// @description fast_4match_start();
/// @function fast_4match_start
function fast_4match_start() {
	/*
	var match = instance_create(0,0,match_obj);

	// PLAYERS
	for(i=1; i<=4; i++)
	{
	    var player = instance_create(0,0,player_obj);
	    player.number = i;
	    player.flag = "number" + string(i) + "_flag";
	    player.icon = DB.bf_icon_map[? player.flag];
	    player.name = "P"+string(i);
	    player.control_set = gamepad;
	    player.control_index = i-1;
    
	    match.players[? i] = player.id;
	}

	match.player_count = 4;
    
	// LIMITS
	for(i=0; i<DB.limit_count; i+=1)
	{
	    limit_name = DB.limit_ids[| i];
	    switch(limit_name)
	    {
	        case "score":
	            match.limit_active[? limit_name] = true;
	            match.limits[? limit_name] = DB.limit_values[?i];                                  
	            break;
	        /*
	        case "sudden_death":
	            match.limit_active[? limit_name] = true;
	            ds_map_replace(match.limits,limit_name,13);                                  
	            break;
	        */
	        /*
	        default:
	            match.limit_active[? limit_name] = false;
	            break;
	    }
	}
   
	// match
 
	//match.starting_slots = 3;

	match.arena = ds_map_find_value(DB.arenas,1);
	match.arena_name = ds_list_find_value(DB.arena_names,1);

	//match.simple_mode = true;
	//match.bolt_rain = true;

	goto_arena();
	*/


}
