function add_hardpoint(x_pos, y_pos, type) {
    hardpoint_x[? hardpoint_count] = x_pos;
    hardpoint_y[? hardpoint_count] = y_pos;
    hardpoint_type[? hardpoint_count] = type;
    hardpoint_item[? hardpoint_count] = noone;

    hardpoint_count++;
}
