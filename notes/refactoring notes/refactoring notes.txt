Miscelaneous:
-------------
* It isn't the best to use `left` `right` `up` `down` and others as global macros...

* start using nullish operators `??`, `??=`

* `basic_bot` and `shield_obj`
share same camera related code

* `overhead_overlay`
`Draw_GUI` needs refactoring

* `border_width` variable
on `healthbar_overlay` doesn't have the same logic as in `empty_frame`
because `healthbar_overlay` overrides parent's `Draw GUI` event without inheriting it

* `gui_label` and `gui_pane`
have duplicate centering code

* `RulePresetsDB`, `RuleCategoriesDB` - rename variable `count`

* `beam_obj`
has a lot of repeated code, refactor to methods

* `black_aoe_obj`
rework to use `motion_add` instead of `gravity_direction`, like `black_projectile_obj`

* `has_not_left_list`, `has_left_inst`
in `projectile_obj`

* `obj_group_obj`
stop using `ds_*` types

* `string_explode`
should output array not list

* "ASSIGN BLOCK" Alarm Events
have a lot of of repeating code

* `c_ltgray -` and `- c_ltgray`
should be replaced with `makeTextOutlineColor` (maybe rename it to just `makeOutlineColor`)

* `gui_color_power_pane`
has some repeating code

* `effect_create_above`
is deprecated

Convert Strings used as Enums to Enums:
---------------------------------------
(but store the String to Enum mapping somewhere)

* `gui_object.rel_position`
* mods / rules - should be dynamic Enum
* `singleton_obj.show_console`
* `damage_popup_obj.type`
* `last_attacker_update` function parameters `target`, `type`
* `battlefeed_item_obj.setValuesAtIndex` function parameters `type`, `content`
* `gamemode_obj.score_values` keys
* `find_display_instance` - `display_type`
* Player level IDs


Convert these to Structs:
-------------------------
* `transform_memory`
* `"params"`
* `last_attacker_*` system
* `last_standing_position` variable
* `hardpoint_*` maps
* `gamemode`
* quest system `transition`, `condition`, `effect`, `state_properties`
* `message_*` scripts
* `anim_step`
