Miscelaneous:
-------------
* `basic_bot` and `shield_obj`
share same camera related code

* `overhead_overlay`
`Draw_GUI` needs refactoring

* `black_aoe_obj`
has some repeating code

* `black_force_field_obj` and `black_projectile_obj`
share some code in `with(item_obj)` section

* `black_aoe_obj` and `black_projectile_obj`
share code for returning guy to normal gravity

* `cannon_*` mechanics scripts
can be methods on `cannon_base_obj` object


Convert Strings used as Enums to Enums:
---------------------------------------
(but store the String to Enum mapping somewhere)
* `DB.console_mode`


Convert these to Structs:
-------------------------
* `transform_memory`
* `DB.ds_registry` entries
* `"results"` and `"params"`
* `last_attacker_*` system
