/*
var ter = place_meeting(x, y, terrain_obj);
var field = place_meeting(x, y, gate_field_obj);

if(instance_exists(ter) || instance_exists(field))
{
    if(instance_exists(ter))
    {
        my_move_bounce(ter);
    }
    else if(instance_exists(field))
    {
        my_move_bounce(field);
    }
}
*/

event_inherited();