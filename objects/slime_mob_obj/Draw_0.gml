event_inherited();

/*
if(DB.console_mode == "debug")
{
    if(instance_exists(blocking_terrain))
    {
        draw_set_colour(c_red);
        my_draw_text(blocking_terrain.x+2, blocking_terrain.y+2, "B");
    }
    if(instance_exists(head_terrain))
    {
        draw_set_colour(c_white);
        my_draw_text(head_terrain.x+2, head_terrain.y+2, "H");
    }
    if(instance_exists(next_terrain))
    {
        draw_set_colour(c_green);
        my_draw_text(next_terrain.x+2, next_terrain.y+2, "N");
    }
}
*/