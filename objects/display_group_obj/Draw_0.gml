/*
var count = ds_map_size(order);
draw_set_colour(c_yellow);
for(i=1; i < count; i++)
{
    prev_disp = order[? i-1];
    disp = order[? i];

    if(instance_exists(disp))
    {
        draw_line(prev_disp.x, prev_disp.y, disp.x, disp.y);
    }
}
*/
